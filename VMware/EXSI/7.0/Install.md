# Input this command so that the VMFSL does not baloon over 100GB

When the installer boots up, use SHIFT + O to go into boot options.
Add this line to set VMFSL to 8 GB: autoPartitionOSDataSize=8192
